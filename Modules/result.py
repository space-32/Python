#import the module

import calc #name of the module is calc i.e. calc file
print(calc.add(3,4)) #print bcz in calc our function is returning values so we have to print them here and need to specify from which module this functions are coming
print(calc.sub(2,3))
calc.greet('sam') #didnt use print bcz we've used print in calc
print(calc.person['age'])
print(calc.lst[1])
